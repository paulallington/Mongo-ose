{
  "name": "mongo-ose",
  "version": "1.0.6",
  "private": true,
  "description": "Mongo-ose - Lightweight MongoDB management tool",
  "main": "electron/dist/main.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build && cd .. && node scripts/bundle-server.mjs && cd electron && npx tsc && cd ..",
    "start": "electron .",
    "package": "npm run build && electron-builder --win",
    "setup": "npm install && cd server && npm install && cd ../client && npm install && cd .. && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "esbuild": "^0.27.4",
    "sharp": "^0.34.5",
    "typescript": "^6.0.2"
  }
}
